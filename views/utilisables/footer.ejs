<footer class="footer">
  <div class="footer-container">

    <!-- ═══════════════════════════════════
         TOP — Brand + Navigation + Contact
         ═══════════════════════════════════ -->
    <div class="footer-top">

      <!-- BRAND -->
      <div class="footer-brand">
        <a href="/" class="footer-logo">
          <img src="/images/logo.png" alt="DevPioneers" onerror="this.style.display='none'">
          <span>Dev<em>Pioneers</em></span>
        </a>
        <p>Nous créons des solutions digitales modernes, élégantes et performantes pour propulser votre business.</p>

        <!-- Réseaux sociaux sous le texte brand -->
        <div class="footer-socials">
          <a href="#" aria-label="Facebook"  class="social-btn">
            <img src="/images/fb.png" alt="Facebook">
          </a>
          <a href="#" aria-label="Twitter/X" class="social-btn">
            <img src="/images/Twitter.png" alt="Twitter">
          </a>
          <a href="#" aria-label="YouTube"   class="social-btn">
            <img src="/images/yt.png" alt="YouTube">
          </a>
          <a href="#" aria-label="Instagram" class="social-btn">
            <img src="/images/ig.png" alt="Instagram">
          </a>
        </div>
      </div>

      <!-- NAVIGATION -->
      <div class="footer-col">
        <h4>Navigation</h4>
        <nav>
          <a href="/">Accueil</a>
          <a href="#services">Services</a>
          <a href="/portfolio">Portfolio</a>
          <a href="/contact">Contact</a>
        </nav>
      </div>


      <!-- CONTACT -->
      <div class="footer-col">
        <h4>Contact</h4>
        <nav>
          <a href="tel:+221710434772" class="contact-item">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
              <path d="M22 16.92v3a2 2 0 01-2.18 2 19.79 19.79 0 01-8.63-3.07A19.5 19.5 0 013.07 10.8 19.79 19.79 0 0120 2.18 2 2 0 0122 4.18v3"/>
            </svg>
            +221 71 043 47 72
          </a>
          <a href="mailto:devpioneershome@gmail.com" class="contact-item">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
              <rect x="2" y="4" width="20" height="16" rx="2"/>
              <polyline points="2,4 12,13 22,4"/>
            </svg>
            devpioneershome@gmail.com
          </a>
          <a href="#" class="contact-item">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
              <path d="M21 10c0 7-9 13-9 13S3 17 3 10a9 9 0 0118 0z"/>
              <circle cx="12" cy="10" r="3"/>
            </svg>
            Dakar, Sénégal
          </a>
        </nav>
      </div>

    </div>

    <!-- ═══════════════════════════════════
         BOTTOM — Copyright
         ═══════════════════════════════════ -->
    <div class="footer-bottom">
      <p>&copy; 2026 DevPioneers — Tous droits réservés.</p>
      <div class="footer-bottom-links">
        <a href="#">Politique de confidentialité</a>
        <a href="#">Mentions légales</a>
      </div>
    </div>

  </div>
</footer>

<style>
/* =============================================
   VARIABLES
   ============================================= */
:root {
  --footer-bg:      #06052e;
  --footer-surface: rgba(255,255,255,0.04);
  --footer-border:  rgba(255,255,255,0.07);
  --footer-muted:   rgba(255,255,255,0.45);
  --footer-text:    rgba(255,255,255,0.7);
  --footer-white:   #ffffff;
  --accent:         #6366f1;
  --accent-light:   #818cf8;
  --transition:     0.28s cubic-bezier(0.23, 1, 0.32, 1);
}


/* =============================================
   FOOTER — WRAPPER
   ============================================= */
.footer {
  background: var(--footer-bg);
  color: var(--footer-text);
  padding: 80px 24px 0;
  position: relative;
  overflow: hidden;
  font-size: 15px;
  line-height: 1.6;
}

/* Ligne lumineuse en haut */
.footer::before {
  content: '';
  position: absolute;
  top: 0; left: 0; right: 0;
  height: 1px;
  background: linear-gradient(90deg,
    transparent 0%,
    rgba(99,102,241,0.5) 40%,
    rgba(129,140,248,0.6) 50%,
    rgba(99,102,241,0.5) 60%,
    transparent 100%);
}

/* Lueur décorative en arrière-plan */
.footer::after {
  content: '';
  position: absolute;
  bottom: -120px;
  right: -120px;
  width: 480px;
  height: 480px;
  border-radius: 50%;
  background: radial-gradient(circle, rgba(99,102,241,0.07) 0%, transparent 70%);
  pointer-events: none;
}

.footer-container {
  max-width: 1200px;
  margin: 0 auto;
  position: relative;
  z-index: 1;
}


/* =============================================
   TOP — GRILLE 4 COLONNES
   ============================================= */
.footer-top {
  display: grid;
  grid-template-columns: 1.8fr 1fr 1fr 1.3fr;
  gap: 48px;
  padding-bottom: 56px;
  border-bottom: 1px solid var(--footer-border);
}


/* ── BRAND ── */
.footer-brand {
  display: flex;
  flex-direction: column;
  gap: 16px;
}

/* Logo texte + image */
.footer-logo {
  display: inline-flex;
  align-items: center;
  gap: 10px;
  text-decoration: none;
  margin-bottom: 4px;
}

.footer-logo img {
  width: 36px;
  height: 36px;
  object-fit: cover;
}

.footer-logo span {
  font-size: 22px;
  font-weight: 800;
  color: var(--footer-white);
  letter-spacing: -0.5px;
}

.footer-logo span em {
  color: var(--accent-light);
  font-style: normal;
}

.footer-brand p {
  color: var(--footer-muted);
  font-size: 14px;
  line-height: 1.75;
  max-width: 300px;
}


/* ── RÉSEAUX SOCIAUX (sous brand) ── */
.footer-socials {
  display: flex;
  gap: 10px;
  margin-top: 4px;
}

.social-btn {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 40px;
  height: 40px;
  border-radius: 10px;
  background: var(--footer-surface);
  border: 1px solid var(--footer-border);
  transition: background var(--transition), border-color var(--transition), transform var(--transition);
}

.social-btn img {
  width: 18px;
  height: 18px;
  object-fit: contain;
  filter: brightness(0.7);
  transition: filter var(--transition);
}

.social-btn:hover {
  background: rgba(99,102,241,0.15);
  border-color: rgba(99,102,241,0.35);
  transform: translateY(-3px);
}

.social-btn:hover img {
  filter: brightness(1);
}


/* ── COLONNES (nav, services, contact) ── */
.footer-col h4 {
  color: var(--footer-white);
  font-size: 12px;
  font-weight: 700;
  letter-spacing: 1.5px;
  text-transform: uppercase;
  margin-bottom: 22px;
  position: relative;
  padding-bottom: 12px;
}

/* Trait coloré sous le titre */
.footer-col h4::after {
  content: '';
  position: absolute;
  bottom: 0; left: 0;
  width: 24px; height: 2px;
  border-radius: 2px;
  background: var(--accent);
}

.footer-col nav {
  display: flex;
  flex-direction: column;
  gap: 4px;
}

/* Liens standards */
.footer-col nav a {
  color: var(--footer-text);
  text-decoration: none;
  font-size: 14px;
  padding: 5px 0;
  display: flex;
  align-items: center;
  gap: 8px;
  transition: color var(--transition), transform var(--transition);
  position: relative;
}

.footer-col nav a:not(.contact-item)::before {
  content: '›';
  color: rgba(99,102,241,0.4);
  font-size: 16px;
  line-height: 1;
  transition: color var(--transition), transform var(--transition);
  flex-shrink: 0;
}

.footer-col nav a:not(.contact-item):hover {
  color: var(--footer-white);
  transform: translateX(4px);
}

.footer-col nav a:not(.contact-item):hover::before {
  color: var(--accent-light);
}

/* Liens de contact avec icône SVG */
.contact-item {
  color: var(--footer-text) !important;
  font-size: 13.5px !important;
  align-items: flex-start !important;
  padding: 6px 0 !important;
}

.contact-item svg {
  width: 15px;
  height: 15px;
  flex-shrink: 0;
  margin-top: 2px;
  color: var(--accent-light);
  transition: transform var(--transition);
}

.contact-item:hover {
  color: var(--footer-white) !important;
  transform: none !important;
}

.contact-item:hover svg {
  transform: scale(1.15);
}


/* =============================================
   BOTTOM — Copyright + liens légaux
   ============================================= */
.footer-bottom {
  display: flex;
  align-items: center;
  justify-content: space-between;
  flex-wrap: wrap;
  gap: 12px;
  padding: 24px 0 32px;
}

.footer-bottom p {
  font-size: 13px;
  color: var(--footer-muted);
}

.footer-bottom-links {
  display: flex;
  gap: 24px;
}

.footer-bottom-links a {
  font-size: 13px;
  color: var(--footer-muted);
  text-decoration: none;
  transition: color var(--transition);
}

.footer-bottom-links a:hover {
  color: var(--footer-white);
}


/* =============================================
   RESPONSIVE — tablette
   ============================================= */
@media (max-width: 960px) {
  .footer-top {
    grid-template-columns: 1fr 1fr;
    gap: 40px;
  }

  /* Brand prend toute la première ligne */
  .footer-brand {
    grid-column: 1 / -1;
  }

  .footer-brand p {
    max-width: 100%;
  }
}


/* =============================================
   RESPONSIVE — mobile
   ============================================= */
@media (max-width: 600px) {

  .footer {
    padding: 60px 20px 0;
  }

  .footer-top {
    grid-template-columns: 1fr;
    gap: 36px;
    padding-bottom: 40px;
  }

  .footer-brand {
    grid-column: auto;
  }

  /* Centrer le brand sur mobile */
  .footer-brand,
  .footer-logo,
  .footer-socials {
    align-items: center;
    text-align: center;
  }

  .footer-brand p {
    text-align: center;
  }

  /* Cacher le trait du titre en mobile centré */
  .footer-col h4 {
    text-align: center;
  }

  .footer-col h4::after {
    left: 50%;
    transform: translateX(-50%);
  }

  .footer-col nav {
    align-items: center;
  }

  .footer-col nav a:not(.contact-item) {
    justify-content: center;
  }

  .footer-col nav a:not(.contact-item):hover {
    transform: none;
  }

  .contact-item {
    justify-content: center !important;
  }

  .footer-bottom {
    flex-direction: column;
    align-items: center;
    text-align: center;
    padding: 20px 0 28px;
  }

  .footer-bottom-links {
    gap: 16px;
  }
}
</style>